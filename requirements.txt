# Python dependencies for the whole repository (problems 1-4).
#
# Verified on Python 3.12; problems 1, 2 and 4 also run on 3.9, while problem 3
# needs 3.10 or newer.
# A conda specification with the same content is available per problem
# (Q1/environment.yml, Q2/environment.yml, Q4/environment.yml).
#
#   python -m pip install -r requirements.txt
#
# matplotlib needs a writable cache directory on some machines; the runners set
# MPLCONFIGDIR for you, or set it yourself:
#
#   Windows PowerShell:  $env:MPLCONFIGDIR = "$PWD\.mplcfg"
#   POSIX shells:        export MPLCONFIGDIR=$PWD/.mplcfg

# Floor versions below are the oldest combination the pipeline has been run on;
# newer releases work too.
numpy>=1.26
scipy>=1.12
scikit-learn>=1.3
matplotlib>=3.8
pandas>=2.2
seaborn>=0.13
