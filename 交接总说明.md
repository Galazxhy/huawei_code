# Q3 → Q4 交接总说明（V005）

## 1. 版本与来源

本包来自：`/home/pz/F_code/F题/PZ_Q3/04_版本实验/V005/PZ_Q3`。

V005最终交付索引：`06_交付/最终交付索引.json`（源目录）。论文PDF 19页，Word 20页；LaTeX实际用XeLaTeX编译通过，源PDF哈希绑定记录在 `05_论文/第三问论文.build.json`。

## 2. 当前定义，队友不得混淆

设训练域为17个，评测域为13个。

### 2.1 配比

\[
\mathbf p=(p_1,\ldots,p_{17}),\quad p_i\ge0,\quad\sum_i p_i=1.
\]

`p` 是实际优化变量，但 V005 将其限制在 A4/A5 训练配方的凸包/缩减锚点域内，不允许在完整单纯形上任意取顶点。

### 2.2 基础质量与治理质量

第一问交接的基础质量是固定向量：

\[
\mathbf q^0=(q^0_1,\ldots,q^0_{17}).
\]

它有17个条目但只有6个不同数值，约为 `0.4785–0.7095`。V005没有优化这些逐域 `q_i^0`。

V005另定义一个标量治理质量：

\[
Q\in[Q_0,1],\qquad Q_0=\frac1{17}\sum_iq_i^0\approx0.6219326581.
\]

这个 `Q` 是B7质量律提供的全局质量桥接变量，不等于17维Q17，也不表示17个领域都达到Q。它通过比值或加性桥接调整13个Loss。

**如果 Q4 要优化逐域质量向量，必须新建模型和数据合同，不得只把标量Q改成数组。**

## 3. V005模型合同

第二问给出13域配比响应：

\[
L_{2,k}(N,D,\mathbf p;\mathbf q^0)=E_k+A_k n^{-\alpha}+B_kd^{-\beta}+S_k(N,D)\Phi_k(\mathbf p,\mathbf q^0),
\]

其中 `n=N/1e9`、`d=D/1e9`；配比特征包含17维 `p_i q_i^0` 和17维 `clr(p)`，共34维。

B7质量律：

\[
F_7(n,d,Q)=0.9654077057+0.5307187920n^{-0.2825}+1.6157571765(dQ)^{-0.108}.
\]

主桥接为：

\[
L_{3,k}=L_{2,k}\left[\frac{F_7(n,d,Q)}{F_7(n,d,Q_0)}\right]^\kappa,
\quad \kappa=1.
\]

训练、注意力、质量处理成本：

\[
C=D[(6+\eta L_{ctx})N+h(Q)],\quad h(Q)=[g(Q)-g(Q_0)]_+,\quad\eta=2\times10^{-4}.
\]

上下文 `L_ctx` 外生取 C7 的 `2048,4096,8192,32768,131072`；临界值 `6/eta=30000`。

目标为13个评测Loss等权均值。V005做了多起点SLSQP、COBYQA对照、有限配方扩展、成本/桥接/零收益消融、近等价与结构识别。

## 4. V005结果应如何表述

- 论文主结论是条件优化结果，不是实测训练收益。
- `Q=1` 只能写成“在给定B7质量标尺和成本假设下达到上界”，不能写成现实数据质量为完美。
- V005没有完成真实治理FLOPs、可达质量上限、Q17到B7共同标尺的独立校准。
- 论文报告没有稳健的联合结构性转移；固定配方在部分情景有阈值敏感事件。
- 训练配方/响应的统计模型存在跨尺度外推；应保留 `solution_claim=best converged local candidate; no global certificate`。

## 5. Q4优先任务

### A. 先完成定义审计

核对Q4是否研究：

1. 延续V005的标量治理Q；或
2. 改为逐域质量 `q=(q_1,...,q_17)`。

二者不能混写。若选第2种，需在论文和代码中删除/改写所有把 `Q` 当标量的桥接公式、成本函数、边界分析和结果表。

### B. 获取逐域质量优化所缺的数据

至少需要同一训练流水线下的配对干预：固定 `N,D,p,L_ctx`，对单个领域施加轻/中/强治理，记录：

- 治理前后原文和22项质量指标；
- Q1压缩、冲突修正后的逐域质量；
- 每域治理处理量、实际FLOPs/时间和成本单位；
- 同评测集的13维Loss；
- 多随机种子及治理前后配对ID。

最小可行设计：选6个现有质量层级，每层选代表域；设置原始、轻、中、强4级治理；至少2个N/D锚点、2个配比和3个训练种子。先做单域治理，再做两域联合，以判断质量-配比交互是否可识别。

### C. 重新检查Q1/Q2可识别性

当前A配比记录中的 `q_i` 固定，无法仅凭 `p_iq_i` 识别独立 `∂L/∂q_i`。需要在拟合中采用交叉配对治理数据，否则只能报告敏感性，不得报告因果质量边际收益。

### D. 复现和扩展规则

- 使用 `/ExtHDD/Apps/Anaconda/envs/llm_gnn/bin/python`。
- 先按源目录 `results/复现清单.json` 检查依赖和哈希。
- 不覆盖V005；Q4新结果写入独立目录。
- 不把Q4新假设反写为V005已验证结论。
- 原始附件保持只读；只提交代码、交接和可追溯的小型结果表。

## 6. 目录说明

|目录|内容|
|---|---|
|`01_阅读入口`|本说明、论文导航|
|`02_模型接口`|Q1/Q2输入和Q3求值器、成本函数、上下文合同|
|`03_代码`|Q3模型、优化、实验注册、审计和结果复核代码|
|`04_关键结果`|论文中使用的核心结果源表|
|`05_论文`|V005 PDF、Word、LaTeX主文件和编译记录|
|`06_质检与哈希`|复现清单、合同/回归/结构/求解器质检|

## 7. 大文件说明

完整结构搜索结果没有复制到本Git包：源文件为 `PZ_Q3/results/q3_structure_shift.json`，大小约169.6MB，SHA-256为 `d35f6c33aadbfd025162619591b953a2e0578bb26d2126a5df87eb992818789b`。需要逐条搜索记录时，应在共享工作区读取源文件并先校验该哈希；不要把精简交接包中的缺失误认为实验未执行。
