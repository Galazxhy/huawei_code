# Q3 → Q4 交接包：V005

这是问题三 V005 最终条件优化实验的交接包，供下一位成员在此基础上继续审计、补证据和扩展实验。

## 先读什么

1. `交接总说明.md`：变量定义、模型边界、结果口径和下一步任务。
2. `02_模型接口/`：Q1/Q2 输入、13维 Loss 求值器、成本函数和上下文约束。
3. `03_代码/`：优化、实验注册、求值审计和结果复核代码。
4. `04_关键结果/`：论文中使用的关键 CSV 结果来源。
5. `05_论文/第三问论文_V005.pdf`：当前论文参考版。
6. `06_质检与哈希/`：复现清单、合同审计、回归审计和求解器对照。

## 重要边界

- 当前 V005 的 `P` 是17维领域配比向量，实际优化。
- 当前 V005 的 `q_i` 是固定的17域基础质量向量；当前优化的 `Q` 是标量治理情景，不是17维质量向量。
- 这不是实测训练结果，而是基于Q2响应和B7质量律桥接的条件优化。
- 没有证明全局最优，没有真实训练优化配置，也没有完成质量治理投入的独立校准。
- 原始附件和完整大体量实验结果不放入此Git交接包；路径、哈希与复现命令见 `交接总说明.md` 和 `V005_复现清单.json`。
